<% if @comment.persisted? %>
  <%= turbo_stream.prepend "comments", partial: "comments/comment", locals: { comment: @comment } %>
  <%= turbo_stream.replace "comment_form_#{dom_id(@post)}", partial: "comments/form", locals: { comment: build_comment, post: @post } %>
<% end %>
