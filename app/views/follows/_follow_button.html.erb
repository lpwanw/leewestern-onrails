<%= link_to follow_user_path(user),
            id: dom_id(user, :follow_button),
            data: { turbo_method: :post },
            class: "w-full bg-black text-center border border-black text-white px-4 py-2 rounded-xl hover:bg-gray-800" do %>
  Follow
<% end %>
